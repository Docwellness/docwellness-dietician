## 1. Backend — serving-unit helper

- [x] 1.1 Create `docwellness-backend/utils/servingUnits.js` exporting `COUNTABLE_SERVING_UNITS = ['piece','nos','roti','slice','egg']`, `isCountableServing(component)` (returns `false` for `undefined`/multi-component/non-listed unit, case-insensitive), and `snapCountablePortion(qty)` = `Math.max(1, Math.round(qty * 2) / 2)`. Verify: new `docwellness-backend/tests/servingUnits.test.js` covers `0.58 → 1`, `1.72 → 1.5`, `2.1 → 2`, `undefined → not countable`, `{unit:'bowl'} → not countable`, `{unit:'PIECE'} → countable`.

## 2. Backend — auto-balance flooring, snapping, redistribution

- [x] 2.1 In `services/ingredientAutoBalanceService.js::autoBalanceIngredients`, after the clamped `ratio` is computed, load the current `RecipeVersion`'s `components[0]`; if `isCountableServing` is true, compute `projectedQty = currentComponentQty * ratio`, `snappedQty = snapCountablePortion(projectedQty)`, `effectiveRatio = clamp(snappedQty / currentComponentQty, MIN_SCALE_RATIO, MAX_SCALE_RATIO)`, and build `updatedIngredients` from `effectiveRatio` instead of `ratio`. Set `newVersion._flooredTo = snappedQty` (in-memory, like `_wasScaleClamped`). Verify: unit test — a countable version whose proportional target is 0.58× yields a new version whose `components[0].quantity` is `1` (± rounding) and whose ingredient `rawQuantity` values moved by the same `effectiveRatio`.
- [x] 2.2 In `autoBalanceDay`, replace the single independent-share loop with the D3 partition: (a) skip `locked || pinned` items; (b) split the rest into `countable` / `continuous` via `isCountableServing` on each item's current version; (c) balance every `countable` item to its current proportional share, recording `achievedCalories`; (d) `remaining = targetDailyCalories - lockedCalories - pinnedCalories - Σ countable.achievedCalories`; (e) distribute `remaining` across `continuous` items in proportion to their current calories and balance each; (f) if `continuous` is empty or `remaining <= 0`, leave countable results as-is. Return per-item results including a `flooredTo` field where set. Verify: `tests/ingredientAutoBalanceService.test.js` — a day with one Chapati (countable) + two dals (continuous) whose naive solve would set Chapati to 0.6 piece instead ends with Chapati at 1 piece and the two dals scaled down so the day total is within tolerance.
- [x] 2.3 Add a test for the no-continuous-items case: a day of only countable items where the naive solve floors two of them — assert portions are the snapped values and the day is returned as outside tolerance (no throw, no infinite adjust). Verify: test passes.
- [x] 2.4 Confirm `autoBalanceWeek` needs no change beyond calling the updated `autoBalanceDay`; add/extend a week-scope test asserting flooring applies per day. Verify: test passes.

## 3. Backend — pinning

- [x] 3.1 Add `pinned: { type: Boolean, default: false }` to `models/PlanItem.js` (additive, no migration). Verify: `PlanItem.create({...})` without `pinned` reads back `pinned === false`.
- [x] 3.2 In `controllers/dietician/planItemController.js::createCustomRecipeVersion` and `updateItemRecipeVersion`, set `planItem.pinned = true` before `planItem.save()`. Verify: `tests/dietPlanCleverEndpoints.test.js` — after `POST .../create-custom-version`, the plan item's `pinned` is `true`; after `POST .../update-item-recipe-version`, likewise.
- [x] 3.3 In `services/ingredientAutoBalanceService.js::autoBalanceDay`, change the eligible filter to `!item.locked && !item.pinned && (calories > 0)` and add pinned items' calories to the fixed baseline alongside locked. Verify: test — a day with a pinned Chapati at 1 piece and a `POST .../auto-balance` at day scope leaves the Chapati's `recipeVersionId` unchanged and rebalances only the others.
- [x] 3.4 Add `PATCH /patients/:patientId/diet-plans/:dietPlanId/plan-items/:planItemId` in `routes/dietician.js` + a `setPinned` handler in `planItemController.js` accepting `{ pinned: boolean }`, using `loadOwnedPlanItem` for ownership, returning the updated item. Verify: test — `PATCH { pinned: false }` on a pinned item clears the flag and does not alter `recipeVersionId`; a cross-plan id returns 404.
- [x] 3.5 In `planItemController.js::getWeekPlanItems`, include `pinned: item.pinned` in each mapped item. Verify: `GET .../weeks/:week/plan-items` response items carry `pinned`.

## 4. Backend — generation flooring

- [x] 4.1 In `services/menuGenerationService.js`, after a `PlanItem` is created for a countable recipe whose V1 serving component is already `>= 1`, no action needed; where the selection/target math would represent a sub-1 serving, apply `snapCountablePortion` to the created item's effective portion (generation does no scaling today, so this is a guard — assert V1 components with `quantity < 1` for a countable unit are bumped to 1 at PlanItem creation, or documented as impossible if V1s are always authored `>= 1`). Verify: test in `tests/createDietPlanDataModel.test.js` or a new generation test — a generated countable item never has a serving component below 1.
- [x] 4.2 In `services/recipeVersioningService.js`, where `scaledComponents` is computed in `createCustomVersion`, snap the component quantity via `snapCountablePortion` when `isCountableServing(component)` so a manually-edited countable recipe's "Makes" figure also stays on 0.5 steps. Verify: unit test — editing a Chapati's flour to a value that yields `1.3` pieces stores `components[0].quantity === 1.5`.

## 5. Flutter — pinning model, service, Refine Portions UI

- [x] 5.1 In `lib/app/modules/diet_plan_wizard/models/wizard_week_models.dart`, add `final bool pinned;` to `WizardPlanItemV2` (default `false`), parse `json['pinned'] == true`. Verify: `flutter analyze` clean; a fixture with `pinned:true` parses to `true`, absent parses to `false`.
- [x] 5.2 In `lib/app/modules/diet_plan_wizard/services/diet_plan_wizard_service.dart`, add `setPinned({patientId, dietPlanId, planItemId, required bool pinned})` calling the `PATCH .../plan-items/:planItemId` endpoint. Verify: method compiles; manual call toggles the flag (observed via a subsequent `getWeekPlanItems`).
- [x] 5.3 In `lib/app/modules/diet_plan_wizard/controllers/refine_portions_step_controller.dart`, add `Future<void> unpin(WizardPlanItemV2 item)` that calls `setPinned(pinned: false)` then `loadWeekPlanItems()`. Confirm `autoBalanceDay`/`autoBalanceWeek`/`_loadAndAutoBalanceOnce` need no client change (server now skips pinned). Verify: `flutter analyze` clean.
- [x] 5.4 In `lib/app/modules/diet_plan_wizard/views/refine_portions_step_view.dart`'s `_RecipeCard`, when `item.pinned` render an "Edited" badge (tappable) in place of / alongside the current lock icon; tapping shows a short confirm ("Let Auto Adjust tune this recipe again?") then calls `controller.unpin(item)`. Keep the card tappable-to-edit and the swap icon visible for pinned items. Verify: manually — edit a Chapati to 1 piece, return to the list, see the "Edited" badge; tap Auto Adjust for the day and confirm the Chapati stays at 1 piece while other rows change; tap the badge, confirm, then Auto Adjust again and see the Chapati move.

## 6. Flutter — shared wizard visual system

- [x] 6.1 Create `lib/app/modules/diet_plan_wizard/widgets/wizard_theme.dart` with the D5 token `const`s (`plum`, `magenta`, `ink`, `muted`, `surface`, `tint`, `line`, `ok`, `warn`) and text-style helpers. Verify: `flutter analyze` clean; existing screens can import it.
- [x] 6.2 Create `PortionPill` (outline pill using `formatQuantityLabel`), `MacroChipRow` (4-up protein/fiber/carbs/fat, monochrome line icon + value + label), `WizardSectionHeader` (title + optional trailing action) as widgets in `lib/app/modules/diet_plan_wizard/widgets/`. Verify: a throwaway widget test or `flutter analyze` clean + visual check in one screen.
- [x] 6.3 Create `WizardRecipeCard` (name, right-aligned calories, `PortionPill`, optional `MacroChipRow`, optional trailing actions slot, optional "Edited" badge, optional expand) and `MealTimeline` (left hairline rail + node per serving-time, takes `List<(String servingTime, List<Widget> children)>`). Verify: `flutter analyze` clean; renders on the Finalize screen (task 9).
- [x] 6.4 Create `DayGroupSelector` (horizontal chip row of day-groups; 4-segment control where it fits, scrollable where it doesn't; selected + tap feedback). Verify: renders all 4 groups at a narrow phone width with no clip; selecting updates a passed callback.

## 7. Flutter — Generate review restyle (presentation-only)

- [x] 7.1 In `lib/app/modules/diet_plan_wizard/views/generate_review_view.dart`, replace `_ReviewRecipeCard`'s hand-rolled `Container` with `WizardRecipeCard` (swap + remove in the actions slot), and its local `const Color` block with imports from `wizard_theme.dart`. Keep `_DayGroupSegmentedControl` / `_MealSlotWrap` behavior; restyle to match `DayGroupSelector` tokens (or adopt it). Verify: `flutter analyze` clean; manual walkthrough — add, remove, swap, and regenerate (with confirmation) a recipe and confirm identical outcomes to before.
- [ ] 7.2 Visual check the Generate review screen against `design.md` tokens at a narrow phone width — no overflow, all 4 day-groups and 7 slots reachable. Verify: screenshot review.

## 8. Flutter — Timeline & Supplements restyle (presentation-only)

- [x] 8.1 In `lib/app/modules/diet_plan_wizard/views/timeline_step_view.dart`, restyle `_DayGroupSection` / `_SlotRow` using `wizard_theme.dart` tokens and (optionally) `MealTimeline` for the slot list; keep `_SupplementChip` semantics. No controller changes. Verify: `flutter analyze` clean; manual — stage two supplements on one slot, remove one, Continue, and confirm they flush to the backend exactly as before.

## 9. Flutter — Finalize / Review redesign

- [x] 9.1 In `lib/app/modules/diet_plan_wizard/controllers/plan_item_finalize_step_controller.dart`, add a `selectedDayGroup` observable (default to the first day-group with items) and a helper returning that day-group's `WizardDayGroupV2`. Keep `dayCalorieChecks` / `targetCalories` / `canActivate` / `finalizeAndActivate` unchanged. Verify: `flutter analyze` clean.
- [x] 9.2 In `lib/app/modules/diet_plan_wizard/views/plan_item_finalize_step_view.dart`, add `DayGroupSelector` at the top; below it render only the selected day-group as a `MealTimeline` of `WizardRecipeCard`s (portion pill, calories, `MacroChipRow` from `recipeVersion.nutritionPerServing`), supplements within their slot. Move `_FinalizedItemDetail`'s ingredient/step prose behind a per-card expand. Keep the `dayCalorieChecks` rows and the `Confirm & Activate` button/gate exactly. Verify: manual — every day-group's meals reachable via the selector; steps hidden until expanded; a deliberately off-target day disables `Confirm & Activate`; an in-tolerance plan activates.
- [ ] 9.3 Visual check Finalize against the patient Diet Plan view (`Screenshot_20260818-130048.png`) and `design.md` — timeline rail present, macro row reads cleanly, brand palette intact. Verify: screenshot review.

## 10. Verification pass

- [x] 10.1 Backend: `npm test` in `docwellness-backend` green, including the new `servingUnits`, floor/redistribute, and pinning cases. Verify: CI/local test run passes.
- [x] 10.2 Flutter: `flutter analyze` clean across all touched files. Verify: command output.
- [ ] 10.3 End-to-end on the emulator against dev-api: generate a plan → Refine Portions shows no sub-1 piece for any roti/bhakri → edit a Chapati to 1 piece (badge appears) → Auto Adjust keeps it and rebalances others → Timeline and Generate screens behave unchanged → Finalize shows the day-group meal timeline and activates within tolerance. Verify: walkthrough notes + screenshots.
- [ ] 10.4 Deploy order per `design.md` Migration Plan (backend to dev-api first, then prod via Coolify; Flutter build after). Verify: `pinned` present in a live `plan-items` response before shipping the app build that reads it.
