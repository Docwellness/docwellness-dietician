## 1. Model

- [x] 1.1 Add `final String role;` (default `'sub'` when parsing, matching the backend's own schema default) to `WizardIngredientLine` in `lib/app/modules/diet_plan_wizard/models/wizard_week_models.dart`, parsed in `fromJson` from `json['role']`, and included in `copyWith`; verify with a unit test round-tripping a JSON payload with `role: 'core'` and one with the field absent (defaults to `'sub'`) — 5 tests in `test/wizard_ingredient_line_role_test.dart`, all passing. Deliberately NOT included in `toJson` (see field's own doc comment: the server always derives/recomputes `role` itself, matching this class's existing convention for other display-only fields).

## 2. Core-group live recompute

- [x] 2.1 Add `_coreScaleRatio` getter to `_IngredientEditorSheetState` (`ingredient_editor_sheet.dart`), mirroring `_portionScaleRatio`'s summation but filtered to `role == 'core'` on both `_originalIngredients` and `_ingredients`; verify with a widget test asserting it returns `1` when no ingredient has `role == 'core'` — verified indirectly (private getter) via observable behavior: a no-core recipe's ingredients never affect each other on edit. All widget tests in `test/ingredient_editor_core_scaling_test.dart`, passing.
- [x] 2.2 In `_updateQuantity`/`_updateUnit`, when the edited ingredient's `role == 'core'`, after updating that entry recompute every `role == 'sub'` entry's `rawQuantity` as `originalSubQuantity * _coreScaleRatio`... — implemented as `_recomputeSubIngredientsIfCoreChanged`. **Design correction found via testing**: initially skipped the recompute when the ratio was ~1 (mirroring the backend's single-shot-save semantics), but a widget test caught this as wrong for the client's per-keystroke live preview - two sequential core edits that net back to the original total left a sub ingredient stuck at a stale intermediate value instead of resetting to its original. Fixed: always recompute unconditionally (see the function's own updated doc comment for why the backend and this live-preview genuinely need different rules here). All widget tests passing.
- [x] 2.3 Confirm editing a `sub` ingredient never triggers the recompute in 2.2 (only its own entry changes) - verify with a widget test — passing

## 3. Ingredient Editor UI

- [x] 3.1 In `_IngredientRow`, add a small "Core" tag/badge next to the ingredient name when `ingredient.role == 'core'`; verify visually (single-core recipe shows exactly one tag, multi-core recipe shows one per core ingredient) — widget test passing (multi-core case; single-core covered by every other test's setup)
- [x] 3.2 Make a `sub`-role row's quantity `TextField` read-only by default (`readOnly: true` / disabled styling) with a small "Override" tap target that unlocks it for editing for the rest of this sheet's session; verify manually that a fresh sheet open shows sub fields locked and tapping Override unlocks the tapped one — implemented with a small lock/unlock `IconButton`; verified via widget test (readOnly true → tap → readOnly false → typed value honored)
- [x] 3.3 When a core ingredient is edited after a sub ingredient was unlocked via Override in the same session, visually revert that sub ingredient's field back to locked/recomputed (consistent with 2.2's recompute overwriting its value) rather than leaving it looking editable while actually being about to be discarded; verify manually — implemented in `_IngredientRowState.didUpdateWidget`; verified via widget test (override discarded, field reverts to `readOnly: true`)
- [x] 3.4 In `_IngredientRowState`, add a `didUpdateWidget` override that updates `_controller.text` to the new formatted `rawQuantity` when `widget.ingredient.rawQuantity` or `.unit` differs from `oldWidget`'s AND the field is not currently focused; verify manually that a sub ingredient's displayed number visibly updates when a core ingredient is edited, without disturbing focus/cursor on whichever field the dietician is actively typing in — implemented (also added a `FocusNode` for the focus check, not present before); verified via every widget test that reads a recomputed value back from the controller (all use `tester.enterText` on a *different* field than the one being asserted)

## 4. Verification pass

- [x] 4.1 Run `flutter analyze` and confirm no new warnings/errors across all touched files — 0 errors, 0 warnings; only pre-existing `withOpacity` deprecation `info` notices, same class already pervasive throughout this file before this change. Also ran the full repo test suite: 1 pre-existing failure (`test/widget_test.dart`'s generic Flutter-starter-template "Counter" smoke test, unrelated to this app), confirmed via `git stash` against the pre-change baseline (fails identically there). All 13 of this change's own tests pass.
- [ ] 4.2 Manual `flutter run` walkthrough: open a single-core recipe (e.g. Chapati, once the backend change and a re-save have given it a designated core ingredient) and confirm editing the core field live-updates sub fields; open a multi-core recipe (e.g. Mixed Vegetable) and confirm the same for the whole core group; confirm a recipe with no core ingredient designated (not yet migrated) behaves exactly as it does today, with no tags and no recompute — **not performed**: no device/emulator or live backend was available in the implementing session. The interaction logic itself (core-edit → sub recompute, lock/unlock, badge display, no-core no-op) is covered by the 8 widget tests in `test/ingredient_editor_core_scaling_test.dart`, which substitute for but don't replace this manual pass. Left unchecked as an explicit pre-ship QA follow-up on a real device.
