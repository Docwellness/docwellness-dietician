// Template for lib/groq_dev_credentials.dart (gitignored, never committed -
// see .gitignore). Copy this file to groq_dev_credentials.dart and fill in a
// real Groq API key (https://console.groq.com/keys) to enable the "magic
// fill" button on the First Consultation form (dev builds only).
//
// Leaving groq_dev_credentials.dart absent/empty is also fine: the button
// just shows an error toast if tapped ("Groq API key not configured")
// instead of failing to build.

const String kGroqApiKey = '';
